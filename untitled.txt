<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>For Poulomi ❤️</title>

  <style>
    * {
      box-sizing: border-box;
      font-family: "Poppins", Arial, sans-serif;
    }

    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .container {
      background: rgba(255,255,255,0.95);
      padding: 25px;
      border-radius: 20px;
      width: 90%;
      max-width: 360px;
      text-align: center;
      box-shadow: 0 15px 30px rgba(0,0,0,0.2);
      position: relative;
      z-index: 2;
    }

    h1 {
      font-size: 22px;
      color: #333;
      margin-bottom: 10px;
    }

    p {
      font-size: 15px;
      color: #555;
      line-height: 1.4;
    }

    .name {
      color: #ff4d6d;
      font-weight: bold;
    }

    .btn-box {
      margin-top: 25px;
      position: relative;
      height: 60px;
    }

    button {
      padding: 10px 22px;
      font-size: 16px;
      border: none;
      border-radius: 25px;
      cursor: pointer;
    }

    #yes {
      background: #ff4d6d;
      color: white;
    }

    #no {
      background: #ccc;
      color: #333;
      position: absolute;
    }

    .result {
      display: none;
    }

    .heart-big {
      font-size: 40px;
      animation: beat 1s infinite;
    }

    @keyframes beat {
      0%,100% { transform: scale(1); }
      50% { transform: scale(1.3); }
    }

    /* Floating hearts */
    .heart {
      position: absolute;
      bottom: -20px;
      color: #ff4d6d;
      font-size: 18px;
      animation: floatUp 6s linear infinite;
      opacity: 0.7;
    }

    @keyframes floatUp {
      from {
        transform: translateY(0) scale(1);
        opacity: 1;
      }
      to {
        transform: translateY(-120vh) scale(1.5);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <div class="container" id="questionBox">
    <h1>Hey <span class="name">Poulomi Dey</span> ❤️</h1>
    <p>
      I don’t need a special day to love you.<br>
      But today, I just want to ask you something from my heart…
    </p>

    <h1>Do you love me? 💕</h1>

    <div class="btn-box">
      <button id="yes">Yes 💖</button>
      <button id="no">No 🙈</button>
    </div>
  </div>

  <div class="container result" id="resultBox">
    <h1>I knew it 🥹💖</h1>
    <p>
      Thank you for choosing me.<br>
      I promise to always care, support, and love you.
    </p>
    <div class="heart-big">❤️</div>
  </div>

  <script>
    const yesBtn = document.getElementById("yes");
    const noBtn = document.getElementById("no");
    const questionBox = document.getElementById("questionBox");
    const resultBox = document.getElementById("resultBox");

    yesBtn.onclick = () => {
      questionBox.style.display = "none";
      resultBox.style.display = "block";
    };

    function moveNo() {
      const x = Math.random() * 200 - 100;
      const y = Math.random() * 100 - 50;
      noBtn.style.transform = `translate(${x}px, ${y}px)`;
    }

    noBtn.onmouseover = moveNo;
    noBtn.ontouchstart = moveNo;

    // Floating hearts generator
    setInterval(() => {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.innerHTML = "❤️";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.fontSize = Math.random() * 20 + 10 + "px";
      heart.style.animationDuration = Math.random() * 3 + 4 + "s";
      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 7000);
    }, 500);
  </script>

</body>
</html><!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>For Poulomi ❤️</title>

  <style>
    * {
      box-sizing: border-box;
      font-family: "Poppins", Arial, sans-serif;
    }

    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .container {
      background: rgba(255,255,255,0.95);
      padding: 25px;
      border-radius: 20px;
      width: 90%;
      max-width: 360px;
      text-align: center;
      box-shadow: 0 15px 30px rgba(0,0,0,0.2);
      position: relative;
      z-index: 2;
    }

    h1 {
      font-size: 22px;
      color: #333;
      margin-bottom: 10px;
    }

    p {
      font-size: 15px;
      color: #555;
      line-height: 1.4;
    }

    .name {
      color: #ff4d6d;
      font-weight: bold;
    }

    .btn-box {
      margin-top: 25px;
      position: relative;
      height: 60px;
    }

    button {
      padding: 10px 22px;
      font-size: 16px;
      border: none;
      border-radius: 25px;
      cursor: pointer;
    }

    #yes {
      background: #ff4d6d;
      color: white;
    }

    #no {
      background: #ccc;
      color: #333;
      position: absolute;
    }

    .result {
      display: none;
    }

    .heart-big {
      font-size: 40px;
      animation: beat 1s infinite;
    }

    @keyframes beat {
      0%,100% { transform: scale(1); }
      50% { transform: scale(1.3); }
    }

    /* Floating hearts */
    .heart {
      position: absolute;
      bottom: -20px;
      color: #ff4d6d;
      font-size: 18px;
      animation: floatUp 6s linear infinite;
      opacity: 0.7;
    }

    @keyframes floatUp {
      from {
        transform: translateY(0) scale(1);
        opacity: 1;
      }
      to {
        transform: translateY(-120vh) scale(1.5);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <div class="container" id="questionBox">
    <h1>Hey <span class="name">Poulomi Dey</span> ❤️</h1>
    <p>
      I don’t need a special day to love you.<br>
      But today, I just want to ask you something from my heart…
    </p>

    <h1>Do you love me? 💕</h1>

    <div class="btn-box">
      <button id="yes">Yes 💖</button>
      <button id="no">No 🙈</button>
    </div>
  </div>

  <div class="container result" id="resultBox">
    <h1>I knew it 🥹💖</h1>
    <p>
      Thank you for choosing me.<br>
      I promise to always care, support, and love you.
    </p>
    <div class="heart-big">❤️</div>
  </div>

  <script>
    const yesBtn = document.getElementById("yes");
    const noBtn = document.getElementById("no");
    const questionBox = document.getElementById("questionBox");
    const resultBox = document.getElementById("resultBox");

    yesBtn.onclick = () => {
      questionBox.style.display = "none";
      resultBox.style.display = "block";
    };

    function moveNo() {
      const x = Math.random() * 200 - 100;
      const y = Math.random() * 100 - 50;
      noBtn.style.transform = `translate(${x}px, ${y}px)`;
    }

    noBtn.onmouseover = moveNo;
    noBtn.ontouchstart = moveNo;

    // Floating hearts generator
    setInterval(() => {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.innerHTML = "❤️";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.fontSize = Math.random() * 20 + 10 + "px";
      heart.style.animationDuration = Math.random() * 3 + 4 + "s";
      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 7000);
    }, 500);
  </script>

</body>
</html>